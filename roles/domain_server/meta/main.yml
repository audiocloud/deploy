galaxy_info:
  description: Domain Server
  author: Bojan Šernek
  min_ansible_version: "2.9"
  license: MIT
  platforms:
    - name: macOS
      versions:
        - all
dependencies:
  - role: "domain_server_{{ ansible_os_family|lower }}"
