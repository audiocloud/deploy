galaxy_info:
  description: Audio Engine
  author: Bojan Šernek
  min_ansible_version: "2.9"
  license: MIT
  platforms:
    - name: macOS
      versions:
        - all
dependencies:
  - role: audio_engine_darwin
    when: ansible_os_family == "Darwin"
